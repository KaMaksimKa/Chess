<UserControl x:Class="Chess.Views.UserControls.ChessBoardUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400"
             x:Name="UserC">
    <Grid DataContext="{Binding ElementName=UserC}"
          Height="{Binding SizeBoard}" Width="{Binding SizeBoard}">
        <Canvas x:Name="CanvasBack" ></Canvas>
        <Canvas x:Name="CanvasCell"></Canvas>
        <Canvas x:Name="CanvasHints"></Canvas>
        <Canvas x:Name="CanvasEmptyCells"></Canvas>
        <Canvas x:Name="CanvasPieces"></Canvas>
        <Canvas x:Name="CanvasChoicePiece"></Canvas>
    </Grid>
</UserControl>
